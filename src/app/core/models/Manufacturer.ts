export type Manufacturer  = {
    uuid: string;
    name: string;
    phone: string;
    email: string;
    image?: string;
    latitude?: number;
    longitude?: number;
    city?: string;
    description?: string;
}